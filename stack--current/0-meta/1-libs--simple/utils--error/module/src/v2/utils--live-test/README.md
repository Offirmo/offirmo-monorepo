
A utility that will generate a whole range of errors,
to test how your system reacts to it.

TODO

f an AggregateError ↗ is thrown by an RPC method, it is not propagated back to the caller.
  - referrencing itself
The SuppressedError ↗ type from the Explicit Resource Management proposal is not currently implemented or supported in Workers.
Own properties of error objects, such as the cause ↗ property, are not propagated back to the caller
 -- referencing itself

huge error (message, name...)
loop in attributes
